simulation-config{
  base-url = "http://localhost:8080/"
  base-url = ${?BASE_URL}
  max-seats = 100
  max-seats = ${?MAX_SEATS}
  users-per-sec = 10
  users-per-sec = ${?USERS_PER_SEC}
  during-sec = 60
  during-sec = ${?DURING_SEC}
}